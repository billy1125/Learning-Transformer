# 學習文件品質改善計劃書

> 撰寫日期：2026-06-10
> 範圍：`theory/` 下六份文件（01a、01b、02、03、04、05）
> 審查面向：說明講述、數學式、計算範例與歷程、圖表、總結匯總

---

## 一、全局問題（跨文件）

### G1：`03-transformer-architecture.md` 目錄與 header 殘留錯誤

**問題：** 目錄仍列出 §9–§13（反向傳播章節），但這些章節已被拆分到 `05-backpropagation.md`；文件開頭 header 也寫「包含完整反向傳播推導」，與實際內容不符。

**改善：** 刪除目錄 §9–§13，修改 header 說明，指向 05。

---

### G2：圖表（視覺化）嚴重不足

**問題：** 六份文件以文字表格為主，缺乏 ASCII 架構圖與 attention 矩陣示意圖。讀者難以建立空間感，尤其在 Multi-Head Attention、GPT Block 堆疊、LayerNorm 三條梯度路徑這三個地方最為明顯。

**改善：** 在關鍵概念點補充 ASCII 圖（見各文件細項）。

---

### G3：`01a` 與 `01b` 差異未明示

**問題：** 兩份文件的 3-token 計算例子幾乎完全相同，讀者不清楚讀完 01a 後是否還需要讀 01b。

**改善：** 在各自開頭增加「本文件與 01a/01b 的差異」說明，強調 01a 用比喻、01b 給統計推導，讓讀者依程度自行選擇。

---

### G4：`01a` 數學公式含中文全角逗號

**問題：** `$e_{i，j}$`、`$e_{i，j} = \frac{x_i^\top x_j}{\sqrt{d}}$` 等公式使用中文全角逗號，LaTeX 排版錯誤。

**改善：** 全域搜尋替換，將數學公式中的中文逗號改為半形逗號。

---

## 二、各文件個別改善項目

---

### 01a｜直覺版前置知識（761 行）

#### 說明講述
- **問題：** 各節末缺少「銜接語」，讀者不清楚為何要看下一節。
- **改善：** 在每節結尾加一句「到這裡你已經知道…，但還缺…」的銜接提示。

#### 數學式
- **問題：** §8 含 3 處中文全角逗號（LaTeX 錯誤）。
- **改善：** 修正為半形逗號（參照 G4）。

#### 計算範例
- **問題：** §7 只完整計算了 $x_3$ 的輸出 $c_3$，$x_1$ 和 $x_2$ 的結果只提到「對稱」但沒列出。
- **改善：** 補上 $c_1$、$c_2$ 的計算結果，並以表格呈現完整的 $3 \times 3$ attention 矩陣與輸出。

#### 圖表
- **問題：** §7 缺少 attention 權重矩陣的視覺示意。
- **改善：** 在 §7 Step 2 之後加 ASCII 熱力圖，顯示 A 矩陣三列的比例：

```
     x1      x2      x3
x1 [高 ~57% 低 ~15% 中 ~27%]
x2 [低 ~15% 高 ~57% 中 ~27%]
x3 [低 ~21% 低 ~21% 高 ~57%]
```

#### 總結
- **問題：** 第 10 節有「故事脈絡」清單，但缺少 Naive → QKV 公式對比的收尾。
- **改善：** 加一個對比框，一眼看出升級前後的差異。

---

### 01b｜數學版前置知識（666 行）

#### 說明講述
- **問題：** §4.4 數值對比格式雜亂，計算式換行過多，可讀性低。
- **改善：** 改成兩欄對比表格（左：直接比例，右：Softmax），去除多餘的換行。

#### 數學式
- **問題：** §8 變異數推導只寫結論（$\text{Var} = d$），缺少中間步驟。
- **改善：** 補 $\text{Var}[(x_i)_k(x_j)_k] = \mathbb{E}[x^2]\mathbb{E}[y^2] - 0 = 1$ 的推導過程。

#### 計算範例
- **問題：** §7 的 3-token 例子與 01a 完全相同，沒有加值。
- **改善：** 保留相同例子，但在計算完後加一段「如果改用 QKV（加入 $W_Q, W_K, W_V$），結果如何不同」的對比，讓數學版有獨特貢獻。

#### 圖表
- **問題：** 缺少 Softmax 溫度效應的直覺示意。
- **改善：** 加 ASCII 示意：相同分數在不同溫度下的輸出分佈變化。

#### 總結
- **問題：** 缺少全文「核心結論」收尾框。
- **改善：** 補 boxed 公式 + 5 項要點清單，與 01a 風格一致。

---

### 02｜注意力直覺（I eat fish 範例）（457 行）

#### 說明講述
- **現況：** 每章有「主旨/概念/脈絡/小結」結構，是六份文件中最完整的，維持。

#### 數學式
- **問題：** 第五章翻譯範例中，softmax 的計算結果只給答案（如 `[0.42, 0.16, 0.42]`），沒有展示 exp 計算過程，對高中生來說是跳躍。
- **改善：** §5.4 補一格展開計算：

```
exp(0.707) ≈ 2.03,  exp(0) = 1.00,  exp(0.707) ≈ 2.03
總和 ≈ 5.06
α = [2.03/5.06, 1.00/5.06, 2.03/5.06] ≈ [0.40, 0.20, 0.40]
```

#### 計算範例
- **問題：** §5.3–5.5 每步的輸出向量（如 $O_{我} = [0.84, 0.58]$）缺少直覺解讀，讀者不知道「為什麼這個向量對應到那個詞」。
- **改善：** 每個輸出後加一句說明，例如「$[0.84, 0.58]$ 的第一維較大，對應到 I 的方向，解碼器從詞彙表中選出最接近的中文詞——我」。

#### 圖表
- **問題：** §5.1 缺少 Encoder-Decoder 架構示意圖；§5.7 缺少完整的 $3 \times 3$ 分數矩陣展示。
- **改善：** §5.1 補 ASCII 架構圖；§5.7 前補完整分數矩陣：

```
Q\K      I      eat    fish
我    [1.00,   0.00,   1.00]  →softmax→ [0.42, 0.16, 0.42]
吃    [0.00,   1.00,   1.00]  →softmax→ [0.16, 0.42, 0.42]
魚    [1.00,   1.00,   2.00]  →softmax→ [0.27, 0.27, 0.46]
```

#### 總結
- **現況：** 「常見易混淆點」附錄非常好，保留。
- **改善：** 末尾加「完整流程一覽表」（輸入→Q/K/V→分數矩陣→softmax→加權輸出→詞），幫助讀者串起全章。

---

### 03｜Transformer 架構（475 行）

#### 說明講述
- **問題：** §6.2 Residual Connection 只有兩行公式說明，缺少直覺解釋。
- **改善：** 補 2–3 句白話：「想像每層都有兩條路可以走：一條是做完 Attention 後的新路，一條是跳過直接走回來的舊路。梯度更新時，舊路永遠暢通——這就是 Residual 的作用。」

#### 計算範例
- **問題：** 全文無任何數值例子，Multi-Head Attention 的拼接操作難以具體想像。
- **改善：** §5 補一個 H=2, d=4, d_k=2 的最小例子，追蹤兩個 head 如何分別計算再 concat。

#### 圖表
- **問題：** 缺少 Multi-Head Attention 並行示意圖；缺少 Transformer Block 方塊圖。
- **改善：** §5 加 ASCII 並行圖；§6 加方塊流程圖：

**Multi-Head Attention 示意（§5）：**
```
          X (T×d)
         /    |    \
       W_Q¹  W_K¹  W_V¹     W_Q²  W_K²  W_V²
        |     |     |          |    |     |
       Q¹    K¹    V¹         Q²   K²    V²
        \______|______/         \___|____/
            Attn₁                  Attn₂
              C¹ (T×d_k)           C² (T×d_k)
               \_________________/
                  Concat (T×d)
                       |
                       W_O
                       |
                    Output (T×d)
```

**Transformer Block 方塊圖（§6）：**
```
    輸入 X
      │
      ├──────────────┐
      │              ↓
      │         LayerNorm (Pre-LN)
      │              │
      │     Multi-Head Attention
      │              │
      └──────── + ───┘  ← Residual
                │
      ├──────────────┐
      │              ↓
      │         LayerNorm (Pre-LN)
      │              │
      │        Feed-Forward
      │              │
      └──────── + ───┘  ← Residual
                │
            輸出 Y
```

#### 總結
- **問題：** §8 之後沒有收尾語，讀者讀完不清楚「我現在理解了什麼」。
- **改善：** 加三句話總結 Transformer 的設計哲學：QKV 分離問題、縮放穩定訓練、多頭捕捉多種關係。

#### 修正項
- **問題（P1 優先）：** 目錄列出 §9–§13；header 說「包含完整反向傳播推導」。
- **改善：** 刪除目錄 §9–§13；修改 header，加指向 05 的連結。

---

### 04｜GPT Decoder-Only（358 行）

#### 說明講述
- **現況：** Encoder vs Decoder 對比表格清楚，nanoGPT 代碼解析搭配說明很好，維持。

#### 計算範例
- **問題：** §3 只有遮罩矩陣示意（`[1,0,0,0]` 格式），缺少數值演示（-inf → softmax → 0 的具體計算）。
- **改善：** 補一個 T=3 的完整 masked attention 數值計算，展示：
  1. 原始分數矩陣 $E$（3×3）
  2. 套上遮罩後（上三角變成 -inf）
  3. softmax 後（-inf 變 0，每列和為 1）

#### 圖表
- **問題：** §5.5 的「資料流」只有文字說明，缺少每步的 shape 標注圖。
- **改善：** 補 ASCII 資料流 shape 圖：

```
idx: (B=2, T=256)
  ↓ token_embedding (vocab_size=65, n_embd=384)
(2, 256, 384)
  ↓ + position_embedding
(2, 256, 384)
  ↓ Block × n_layer=6
(2, 256, 384)   ← 每個 Block 都維持相同 shape
  ↓ LayerNorm
(2, 256, 384)
  ↓ lm_head (n_embd=384 → vocab_size=65)
(2, 256, 65)    ← 每個位置預測下一個 token 的機率
```

#### 總結
- **問題：** 速查清單（§9）前缺少一段整體回顧，讀者閱讀清單前沒有複習。
- **改善：** 在 §9 前加 3–5 句：「在打開 NB4 之前，你應該已經能回答以下問題……」

---

### 05｜反向傳播推導（528 行）

#### 說明講述
- **問題：** 全文以符號為主，每個 boxed 公式後缺少白話翻譯，讀者難以確認自己理解正確。
- **改善：** 在每個 boxed 公式後加 1–2 句：例如 $\frac{\partial \mathcal{L}}{\partial V} = A^\top G^C$ 後補「白話：上游梯度按 attention 權重的轉置分配回每個 Value 向量。」

#### 計算範例（最大缺口）
- **問題：** 全文無任何具體數字，讀者無法動手驗證是否理解正確。這是與 NB3 的最大脫節點——文件沒有給讀者「自己先算一遍再對答案」的機會。
- **改善：** 在 §9 開頭加一個迷你例子（T=2, d_k=2）：
  1. 給定具體的 Q、K、V 數值
  2. 算出 E、A、C
  3. 假設 $G^C$ 已知
  4. 步驟追蹤：$G^V$ → $G^A$ → $G^E$ → $G^Q$、$G^K$
  5. 每步給出數字，讀者可以核對

#### 圖表
- **問題：** §13.4 LayerNorm 三條梯度路徑只有文字說明，缺少路徑圖。
- **改善：** 補 ASCII 路徑圖：

```
                     ┌─ 路徑1（直接）─────────── ŷ_j → L
x_j ──→ x_j - μ ───→ ÷r ──→ ŷ_j
  │                    ↑
  │                    │ variance 路徑（路徑3）
  │              σ² → r → (-1/2r³)
  │               ↑
  │    (x_j-μ)²/d ─── 所有 j 累計
  │
  └─ (1/d) → μ ──────────── 影響所有 x_k-μ  （路徑2: mean）
```

#### 總結
- **問題：** Self-Attention 和 LayerNorm 的梯度結論分散在不同章節末，讀者需要自行整合。
- **改善：** 末尾的「核心總結」後補一個「反向傳播完整梯度查閱表」，列出從 $G^C$ 到所有葉節點（$G_{W_Q}, G_{W_K}, G_{W_V}, G_X$）的公式一覽。

#### 文件編號問題
- **問題：** 章節從 §9 開始（繼承自舊 `Transformer.md` 的分割），對新讀者非常困惑。
- **改善：** 重新從 §1 編號（§9→§1, §10→§2, …, §13→§5）。

---

## 三、改善優先順序

| 優先 | 文件 | 問題描述 | 難度 | 狀態 |
|---|---|---|---|---|
| **P1** | 03 | 刪除目錄 §9–§13，修正 header | 低 | ✅ 完成 |
| **P1** | 01a | 修正中文全角逗號（LaTeX 錯誤）| 低 | ✅ 完成 |
| **P2** | 05 | 加數值計算範例（T=2, d_k=2 迷你例）| 高 | ✅ 完成 |
| **P2** | 02 | 補 softmax 展開計算（exp 步驟）+ 修正數值錯誤 | 低 | ✅ 完成 |
| **P2** | 03 | 補 Multi-Head 最小數字例 + ASCII 圖 | 中 | ✅ 完成 |
| **P3** | 05 | 重新從 §1 編號 | 低 | ✅ 完成 |
| **P3** | 04 | 補 masked attention 數值演示（T=3）| 中 | ✅ 完成 |
| **P3** | 01a | 補完整 $c_1, c_2$ 計算 + 熱力圖 | 中 | ✅ 完成 |
| **P3** | 01b | 補變異數推導中間步驟 | 低 | ✅ 完成 |
| **P3** | 03 | 補 Residual 白話說明 + Block 方塊圖 | 中 | ✅ 完成 |
| **P4** | 所有 | 各節銜接語與整體白話收尾 | 中 | ⬜ 待執行 |
| **P4** | 02 | 補完整 $3 \times 3$ 分數矩陣圖 | 低 | ⬜ 待執行 |
| **P4** | 05 | 補 LayerNorm 路徑 ASCII 圖 | 中 | ⬜ 待執行 |
| **P4** | 05 | 補完整梯度查閱表 | 中 | ⬜ 待執行 |

---

## 四、不建議更動的部分

以下內容品質已高，改善風險大於收益：

- **02 第六章「8 項限制對比」**：覆蓋完整，邏輯清晰。
- **05 §13.7 三項語意解析表格**：把 mean/variance 路徑說清楚了。
- **04 §9 速查清單**：7 個問題設計精準，直接對應 NB4。
- **03 §8 RNN vs Transformer 對比表**：資訊密度高，保留原樣。
- **02 附錄「常見易混淆點」**：5 個對比非常實用。

---

*本計劃書作為改善執行的參考依據，每完成一項請在優先順序表中標記完成。*
